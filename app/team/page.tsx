 "use client";import{useEffect,useState}from"react";import{db}from"../../lib/supabase";export default function Team(){const[m,setM]=useState<any[]>([]);const[u,setU]=useState<any>(null);useEffect(()=>{(async()=>{const s=db();const{data:x}=await s.auth.getUser();setU(x.user);const{data}=await s.from("company_members").select("*");setM(data||[])})()},[]);return <><div className="eyebrow">People & Permissions</div><div className="h1">Team</div><p className="muted">Membership and project isolation are enforced by Supabase RLS.</p><div className="panel"><div className="h2">Signed-in User</div><div className="statline"><span>Email</span><b>{u?.email||"—"}</b></div></div><div className="panel"><div className="h2">Memberships</div><div className="tablewrap"><table><thead><tr><th>Company</th><th>User</th><th>Role</th></tr></thead><tbody>{m.map((x,i)=><tr key={i}><td>{x.company_id}</td><td>{x.user_id}</td><td><span className="pill blue">{x.role}</span></td></tr>)}</tbody></table></div></div></> }